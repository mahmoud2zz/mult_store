import 'package:flutter/material.dart';

class PreparingOrders extends StatelessWidget {
  const PreparingOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('Preparing'),),
    );
  }
}
