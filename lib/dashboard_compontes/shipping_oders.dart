import 'package:flutter/material.dart';

class ShippingOrders extends StatelessWidget {
  const ShippingOrders({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(child: Text('Shipping'),),
    );
  }
}
